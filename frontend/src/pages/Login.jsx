function Login() {
  return (
    <section style={cardStyle}>
      <h2>Login</h2>
      <p>Use this page for user sign-in UI and backend authentication wiring.</p>
    </section>
  );
}

const cardStyle = {
  padding: "1.5rem",
  borderRadius: "20px",
  background: "rgba(255, 255, 255, 0.08)",
  color: "#ffffff",
};

export default Login;
